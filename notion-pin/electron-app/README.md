# Nopin desktop application

The user and contributor documentation is in the repository's [main README](../../README.md).

```bash
npm ci
npm run check
npm run dev
```

macOS packages can be built with `npm run build:mac:arm64` or `npm run build:mac:x64`.
